import React from 'react';
import styles from './Nav.scss';

const Nav = () => (
  <nav className={styles.navContainer}>
    <a href="#">work</a>
    <a href="#">about</a>
  </nav>
);

export default Nav;
