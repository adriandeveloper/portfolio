import React from 'react';
import styles from './Nav.scss';

const Nav = () => (
  <nav className={styles.navContainer}>
    <a href="#">ethos</a>
    <a href="#">projects</a>
    <a href="#">contact</a>
  </nav>
);

export default Nav;
