import React from 'react';
import './Nav.scss';

const Nav = () => (
  <nav className="navContainer">
    <a href="#">ethos</a>
    <a href="#">projects</a>
    <a href="#">contact</a>
  </nav>
);

export default Nav;
